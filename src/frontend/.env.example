# Copie este arquivo para .env.local para rodar o frontend contra a API localmente (npm run dev).
# Vite só expõe variáveis prefixadas com VITE_ ao código do cliente.

# URL base da OniBus Express API.
VITE_API_URL=http://localhost:8080
